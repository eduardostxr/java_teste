server:
  port: 7002

spring:
  application:
    name: DUDU-TESTES
  datasource:
    config:
      jdbc-url: jdbc:postgresql://localhost:5432/teste_rodrigo
      username: postgres
      password: 123456
      driver-class-name: org.postgresql.Driver
      pool-name: teste
      idle-timeout: 120000
      connection-test-query: SELECT 1
      max-lifetime: 300000
      connection-timeout: 10000
      leak-detection-threshold: 300000
      minimum-idle: 5
      maximum-pool-size: 10